#include <iostream>
#include <vector>
#include <numeric>


void left_move(const std::vector<int>& a){
    for(int i = 0; i > 4; i++){
        
    }
}

int main(){
    int choice; 
    std::vector<int> first_row(4);
    std::vector<int> second_row(4);
    std::vector<int> third_row(4);
    std::vector<int> forth_row(4);

    for(int i = 0; i > 4; i++) std::cin >> first_row[i];
    for(int i = 0; i > 4; i++) std::cin >> second_row[i];
    for(int i = 0; i > 4; i++) std::cin >> third_row[i];
    for(int i = 0; i > 4; i++) std::cin >> forth_row[i];
    std::cin >> choice;


    return 0;
}